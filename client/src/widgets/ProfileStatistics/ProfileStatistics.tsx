import { useGetProfileByIdQuery } from '../../entities/user/model';

export const ProfileStatistics = () => {
  // const { isError, isLoading, data } = useGetProfileByIdQuery(
  //   '6a4abf540d4f78c5eb505b32',
  // );
  // console.log('-------------', data);

  return <div></div>;
};
